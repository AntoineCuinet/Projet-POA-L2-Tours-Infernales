/**
 * Represents an interface for redirecting the movement of a Moving object.
 */
public interface Redirector {
    /**
     * Redirects the movement of a Moving object.
     *
     * @param m The Moving object whose movement needs to be redirected.
     */
    void redirect(Moving m);
}
